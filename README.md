# From-Kaplan-Meier-Curves-to-Log-Rank-Results
Prototype site uploads Kaplan–Meier images, uses a vision LLM to extract event times and censoring, reconstructs pseudo-data, and runs a two-arm log-rank test in the browser. It also queries PubMed for A–B and B–C studies and combines them for indirect A–C comparisons.
